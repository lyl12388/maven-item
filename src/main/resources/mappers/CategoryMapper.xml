<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.vip.items.mapper.CategoryMapper">
  <resultMap id="base_cate" type="Category">
    <id property="cateId" column="cate_id"/>
    <result property="cateName" column="cate_name"/>
  </resultMap>
  <select id="findByproId" resultMap="base_cate">
    select c.cate_name
    from category c
    where c.cate_id = (select cate_id from project where proid = #{proId})
  </select>

  <select id="findByPid" resultMap="base_cate">
    select c.cate_name
    from category c
    where c.cate_id = (select cate_id from programmer where pid = #{pid})
  </select>
</mapper>